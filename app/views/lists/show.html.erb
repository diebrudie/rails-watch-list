<h1><%= @list.name %></h1>

<%= link_to '🎥', lists_path %>
<%#= link_to '+', new_list_bookmark_path %>
<div class="container">
  <div class="row">
    <div class="col-6">
    <h3>Add a movie to your <%= @list.name %></h3>
      <%= simple_form_for [@list, @bookmark] do |l| %>
        <%= l.input :comment, placeholder: "add soemthing" %>
        <%= l.association :movie %>
        <%= l.submit :submit, class: 'btn btn-primary mt-3'%>
      <% end %>
    </div>
  </div>
</div>
